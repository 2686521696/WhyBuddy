# slide-rule-python - requirements for runnable V5 backend
# Modeled on tws-ai-ask-python structure (FastAPI + services for RAG/LLM/knowledge)
# This makes the Python side actually importable and the /api/sliderule surface testable

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
# 配色合规校验（services/palette_guard.py）：OKLCh 转换与色差。手搓 OKLab
# 矩阵能省这个依赖，但色彩空间转换是那种"差一点点就全错、还很难发现"的活，
# 用成熟库更稳。
coloraide>=4.0
# FreeformInsight 内容树是深层嵌套 JSON，LLM 一次性整段吐出来偶发数错括号
# 层数（真机复现过：{}/[] 各差 1、尾巴多一个孤立句号，且不是 token 截断——
# 那份输出结尾收得完整）。这类"语法错但结构基本对"的输出，成熟的修复库
# 比重新问一轮 LLM 更快更稳，用它而不是自己写括号计数式的线性修补——
# 手搓的线性修补容易把内容拼出语法合法但结构错位的树。
json-repair>=0.61
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.0.0

# RAG / retrieval (real external evidence, replacing Node stubs)
qdrant-client>=1.12.0
openai>=1.50.0  # for LLM calls in stable style (reference project uses similar)

# Persistence (sessions, like reference)
sqlalchemy>=2.0.0
pymysql>=1.1.0
# App Store（生成应用持久化 → 组建库地基）：配了 APP_STORE_DATABASE_URL 指向
# Postgres（Neon/自建）时用 psycopg v3 连（支持 Neon 的 channel_binding）；不配
# 走本地 JSON 文件兜底，这条依赖也不会被 import。
psycopg[binary]>=3.2

# P2b execution tools: code.run runs ONLY inside E2B sandbox (fail-closed without key)
e2b-code-interpreter>=2.8.0
