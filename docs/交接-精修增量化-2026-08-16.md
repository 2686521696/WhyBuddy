# 交接：精修改成增量修改（2026-08-16）

一句话现状（2026-08-17 修正）：**结构已经止住；剩下的主要不是"内容被重写"，
是 id 每轮重铸导致引用全部错位。** 六段里四段是 id 抖动，只有 `datamodel`
和 `aigc` 是真重写。下一件事是 id 稳定化——见文末第五节，**别照第二节做**。

用户的原始诉求是「我在已生成的应用上说一句『某一页的列表是空的，加点模拟
数据』，结果整个应用被换掉了」。

---

## 一、当前状态

### 已经修好（有真机验证）

| 问题 | 提交 | 验证方式 |
|---|---|---|
| p2 菜单圆角失效（外壳依赖的类每页各自定义） | `21048a84` | 拿线上四页原样跑 `unify_shell`，修复前 p2 缺 `rounded-custom`，修复后四页同为 12px |
| 左栏 79 条步骤全标 `intent.parse` | `7ef16bcb` | 源码判据 + 变异 |
| 版本回退每点一次多一版 | `fac9cab0` | 根因：去重**只跟队尾比**，来回点必然交替追加 |
| 回退能连点 / 无效点击零反馈 / 并发竞态 | `49dc7a06` | 前端 in-flight 闸 + 后端每会话串行 + `already_current` 不再静默 |

### 部分修好 —— 这就是下一轮要接着做的

「改一句话把整个应用换掉」修了四次，实测：

```
                       菜单保留    页面数     逐段指纹保留
修复前基线              0 / 4      4 → 3      0 / 6
fef97cb3 之后           3 / 3      3 → 3      0 / 6
040ca58d 之后           4 / 4      4 → 4      0 / 6
```

**结构保住了（两次独立验证都满分），内容没有。** `workflow`、`rbac` 这些跟指令
毫不相干的段照样全变。

> ⚠ **2026-08-17 更正：上面这句「内容没有」只对了三分之一。**
> 「逐段指纹 0/6」这个读数**证明不了**内容被重写——它跟「内容一字没动、
> 只是 id 换了一批」是同一个读数。实测把两者分开之后，六段里四段主要是
> id 抖动。详见文末〈2026-08-17：0/6 的成分〉。这一节以下的方案选型
> （含方案 1/2/3）都建立在那个未经验证的假设上，**别照着往下做**。

四次修复的定位：

```
48ffe604  让指令能到达生成层（此前 blocked 时被整个丢掉）      必要，不够
0f5686e5  精修提示词的收尾不再要求"产出一份"（自相矛盾）        必要，不够
fef97cb3  精修模式提到主循环之前判定                          ★ 保住了结构
040ca58d  Merge Patch 实现（RFC 7386）                        ⚠ 接错了生成器
a24147c2  加判据：合并必须落在真正在跑的那条链上               strict xfail，故意红
```

### 完全没动

- **④ 相关性闸把营销标题切成伪业务点**：`closure_relevance.py` 的 `_SPLIT_RE`
  里没有 `——` 和 `！`，导致「【硬件+社会公益】步伴 AI 拐杖——这一次，我们重新
  定义智能拐杖！」被切成三个伪业务点，覆盖率必然 0% → `CLOSURE_GOAL_RELEVANCE_FAILED`
  硬阻断。⚠ 这道闸是**标定过的**（模块头有标定集和对照表），改它要重跑标定；
  用户线上 29 个真实会话可当标定集。
- **⑤ 三套 turnId 命名并存**：`turnNarrations` 用 `turn-<ts>`、
  `reasoningEvents`/`capabilityRuns` 用 `loop-N`（每轮从 0 重数、跨轮撞号）、
  `lastTurnId` 是 `turn-<ts>-drive-full`。三处对不上账，排查只能手动拼数据。
- **中转站偶发把客服话术当结果返回**：`api.rcouyi.com` 两次把「你好！我是 OuYi
  （欧亿 AI 助手）…」当 JSON 返回，一次让 `report.write` 降级、一次成了主产物。
  **目前没有任何地方统计这类失败的发生率。**

---

## 二、下一轮第一件事

### 目标

精修时，**用户没提到的模型段不许被重写**。验收：`workflow` / `rbac` 在
「给某页加模拟数据」这类指令下指纹**未变**。

### 为什么不是 Merge Patch

已经试过，接不进去。`v5_capability_executor.py:492`：

```python
model = run_spec_first(goal, refine=_spec_refine, ...)["model"]
```

spec-first 天生「从 spec 树重新生成」，出口**永远是完整模型**。合并器看到六段
齐全就判「没按补丁交付」，原样放行。**补丁语义和这条链的架构不兼容**——这不是
接线问题，是方案选错了。

`services/merge_patch.py` 本身是对的（RFC 7386 逐行实现，12 条测试 + 2 个变异
全咬住），留着不删：将来若有真正按补丁产出的生成器，直接可用。

### 正确修法

**沿用同一个文件里已经存在的做法**。`v5_capability_executor.py:478-481`：

```python
if _reuse_style:     print("精修沿用上一版风格段")
if _reuse_language:  print("精修沿用上一版设计语言")
```

「精修沿用上一版」这个模式仓里已经有了，**只是只用在风格段上**。把它扩展到
模型段：精修时未被指令涉及的段**直接从基线复制，不进重新生成**。

思路同 Kubernetes server-side apply 的**字段归属**——谁拥有哪一段，重新生成就
不许覆盖不属于它的部分。是本仓已有的惯用法，不是新发明。

要动的地方：`run_spec_first` 的参数契约 + spec-first 内部的分支。**比之前任何
一次改动都深，别在上下文余量不足时动。**

难点：怎么判断「指令涉及哪几段」。可选方案（按稳妥度）：

1. 让精修的 SPEC 步顺带声明它需要改哪几段（多一个字段，不多一次调用）
2. 默认只允许改 `datamodel` / `page`，其余段一律沿用，用 env 开关控制
3. 关键词启发式（**不推荐**，本仓对未标定的启发式一贯保守）

### 开局三条命令

```bash
git log --oneline -14
slide-rule-python/.venv/bin/python -m pytest slide-rule-python/tests/test_refine_segment_reuse.py -v
cat slide-rule-python/tests/test_refine_merge_patch.py     # 为什么走到这一步
```

⚠ **2026-08-17 更新**：原来那条 strict xfail（`test_refine_merge_reaches_the_live_path`
里的合并判据）**已经摘了**——不是因为 Merge Patch 修好了，是因为换了方案
（补丁 → 逐段沿用），判据也从 grep 换成了行为判据。那个文件现在只守
「哪条链在跑 + 精修上下文有没有到达它」，逐段行为在
`test_refine_segment_reuse.py`（31 条 + 7 个变异）。

⚠ 逐段沿用**已经实现并提交，但真机上是惰性的**（一段都沿用不成，被引用完整性
闸拒绝）。原因见第五节。别以为它坏了要回滚——它在等 id 稳定化。

---

## 三、验收方法（重要，别用错尺子）

**判据是逐段指纹 + 菜单保留数，不是「变没变」。**

早上用「四页字节变没变」验精修，4/4 变了就判通过——那只能证明「有反应」，
**全量重写同样让 4/4 都变**。逐段指纹才看得出是增量还是重写。

第二轮的指令必须针对**第一轮确实存在的页面**。踩过一次：指令写死「预警消息
中心」而第一轮没这一页，模型必须新增，指纹全变里有一部分是合理的，判不出问题。

菜单判据用**保留了几个名字**，不是 `n1 == n2`——加一页不算被换掉。

### 基线数据（都在线上，用户账号 2686521696@qq.com）

| 会话 | 状态 | 结果 |
|---|---|---|
| `sr-20260816165447` | 修复前 | 0/6 段，菜单 0/4 |
| `sr-20260816170934` | 修复前 | 0/6 段，菜单 0/4，还丢一页 |
| `sr-20260816201658` | fef97cb3 后 | 0/6 段，菜单 3/3 |
| 本地一轮 | 040ca58d 后 | 0/6 段，菜单 4/4 |

现成的验证脚本形态见提交历史里的说明；核心是两轮 drive + 逐段 sha256。

⚠ **浏览器在开发容器里出不去**（连 example.com 都 `ERR_CONNECTION_RESET`），
线上验证只能走接口（`/api/sliderule/drive-full-stream`，跟前端同一条）。
同步接口 `/api/sliderule/drive-full` 会被网关掐断长连接——服务端照常跑完，
客户端拿不到结果。

⚠ **本地进程内直驱**（`drive_full_v5_session` + `persist_state` 打成空操作）
是最快的验证方式：真调 LLM，但跑的是工作区代码，**不用等部署**。
Merge Patch 接错插座就是这么发现的。

---

## 四、这一天最该记住的一条

**三次修复都打偏，全都是「没先确认哪条链真的在跑」。**

已经写进 `CLAUDE.md` 第一条，也具象成了
`tests/test_refine_merge_reaches_the_live_path.py`。下次改生成相关的东西，
**先在目标路径打一行日志确认它被执行到**，再动手。

---

## 五、2026-08-17：0/6 的成分（真机实测，推翻了第一节的判断）

### 先说结论

做完方案 1（逐段沿用）之后真机跑了两轮，**沿用一段都没成功**——四次尝试全被
引用完整性闸拒绝。追下去发现根因不在沿用本身，而在一件从没被验证过的事：

> **id 每一轮都重新铸造。** 于是「逐段指纹 0/6」根本证明不了内容被重写——
> 它跟「内容一字没动、只是 id 换了一批」是同一个读数。

把两者分开量（社区养老平台，两轮，`page` 都是 5 页）：

| 对象 | 个数 | id 保住 | 名字保住 | 判断 |
|---|---|---|---|---|
| `page.pages` | 5→5 | **5/5** | **5/5** | **完全稳定**，指纹变纯粹因为引用了被改名的权限 |
| `rbac.roles` | 3→3 | **0/3** | **3/3** | **纯 id 抖动**：同样三个角色，id 全换 |
| `workflow.nodes` | 4→3 | 0/4 | 3/4 | 主要是 id 抖动，另少一个节点 |
| `appbundle` | — | — | — | 整段都是引用，跟着上面一起变 |
| `rbac.permissions` | 14→11 | 4/14 | — | 混合：改名 + 增删 |
| `datamodel.entities` | 5→4 | 1/5 | 2/5 | **真的重写**：`mall_service` 整个消失，字段成套换掉 |
| `aigc.capabilities` | 1→1 | 0/1 | 0/1 | **真的重写**：能力完全不同 |

id 漂移长这样（同一个角色，两轮三套名字）：

```
station_manager  →  role_station_manager  →  manager_role      「社区养老站长」
elder:read       →  care_order:read       →  elder_archive:read
wo_created       →  wf_pending            →  node_pending      「待派单」
```

**不是随机重铸，是近义漂移**：id 由概念名派生，派生规则稳定但用词不稳定
（`elder` / `elderly` / `elder_archive` 是同一个东西）。所以它**极易锁定**——
把上一版 id 表喂进去要求沿用即可，不需要重映射机制。

### 根因在链路结构里，一条 grep 就能确认

```bash
grep -c refine services/model_assembly.py services/spec_semantics.py services/html_structure.py
# 三个都是 0
```

refine 上下文**只到达第 2 步（SPEC）**。而铸造实体/角色/权限/流程节点 id 的是
第 4/5/6 步——它们对「有上一版」这件事一无所知。于是：

```
第2步  收到 refine，保住了页面名、角色名（实测 5/5、3/3）   ← 所以结构保住了
第4/5/6步  不知道有上一版，id 全部现铸                      ← 所以指纹全变
```

这也解释了为什么四次修复只保住结构、保不住指纹：**四次都改在第 2 步或它上游。**

### 为什么逐段沿用现在是惰性的

闸守的是引用完整性，而混合两轮的模型必然违反它——两个方向都堵死：

```
沿用 rbac              → 新生成的 page.actionPermissions 指向新权限 id，旧 rbac 里没有
沿用 rbac + workflow   → 同上
只沿用 rbac            → 新生成的 workflow.assigneeRole 指向新角色 id，旧 rbac 里没有
```

调换丢弃顺序救不了——`rbac` 是被依赖最多的那个，而 `page` 永远重新生成。
**只要 id 每轮重铸，任何跨轮混合都过不了闸。**

### 下一件事：id 稳定化（比之前设想的三个方案都轻）

不是改架构，是**补一份 id 表进第 4/5/6 步的提示词**，要求既有概念沿用既有 id。

做完之后：
- `page` / `rbac.roles` / `workflow` / `appbundle` 四段的指纹直接拿回来
- 闸的封锁解开，**已经提交的逐段沿用会自动开始生效**（它现在惰性，但机制和
  失败路径都验证过了，别回滚）
- 剩下 `datamodel` / `aigc` 是真重写，要单独处理。⚠ 注意 `datamodel` 被**排除**
  在可沿用清单之外（它跟第 3 步的 HTML 绑定），所以它需要的是另一套办法，
  不是把它加进沿用清单

### 验收方法要跟着改

**别再单用 raw 逐段指纹当判据**——它把 id 抖动和内容重写混成一个读数，
四次修复的选型就是被它误导的。至少要三把尺子并排：

```
raw          原样 sha256
抹 id 后      id 换成被引对象的名字、列表按内容排序
名字 Jaccard  每段里人类可读名字的集合相似度
```

⚠ 写这把尺子时踩到两个坑，别重犯：
1. 两边各用自己的 id→name 表解引用，会让**同一份字节**被判成"变"
   （引用的对象在新一轮没了）。结果"抹 id 后"比 raw 还低，荒谬。
2. 权限是**裸字符串**（`elder:read`），没有 `name` 字段，永远进不了 id→name 表。
   只靠这把尺子会得出"id 不是主因"的错误结论——**我第一版就是这么错的**，
   是回头直接看 `rbac.roles` 的原始数据（3/3 名字相同、0/3 id 相同）才纠正过来。

**量不清楚的时候，直接把两版数据摊开看比造尺子快。**
