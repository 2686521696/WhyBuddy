# 最近功能与 grok-build 对照审查

审查起点：`6eb7bb59fcab24c3ac8add688de08ba00582d975`。对照源码：工作区 `../grok-build`。图片是审查线索，当前实际代码及测试才是结论依据。

## 清单

| 项目 | 全局链路 | grok 对照与边界 | 状态 |
| --- | --- | --- | --- |
| 1. 控制面与工厂职责 | 输入、工具选择、工厂交接、结果回到控制面 | sampler 主循环与原子工具；保留产品领域执行器 | 通过：统一 control-turn，工具权限集中判定 |
| 2. 通用问卷 | 工具协议、SSE、队列回执、刷新恢复 | AskUserQuestion 类型和格式化结果 | 已修复：问卷严格持久化、重复回执 409、刷新恢复 |
| 3. 计划批准 | 写计划、停泊、批准/修改/退出、实际执行 | exit_plan_mode 读取已保存计划；不让跳过问卷替代批准 | 审查中 |
| 4. 手动暂停 | 请求暂停、先通知、异步等待、恢复、取消 | 等待状态、计时、无人值守与取消区分 | 审查中 |
| 5. 心跳与等待可见性 | worker 泵、业务事件、心跳、运行快照 | 区分没有数据与只有心跳；等待是独立状态 | 审查中 |
| 6. 阶段事件配对 | 开始/完成、并行阶段、取消与异常收尾 | 成对计时及幂等收尾 | 已修复：按 pageId/device 配对并行页面 |
| 7. 页面事件补发 | 产线页面、事件队列、持久化、前端接收 | 实际产出与通知数量对账 | 审查中 |
| 8. 能力运行台账 | 执行结果、状态、耗时、来源、历史兼容 | ToolCallOutcome 必填；旧数据 Unknown | 已修复：顶层与 timing duration 统一 |
| 9. 交付物可用面 | SPEC 需求、承载页、控件、闭环判据 | 量、归类、分层处置；领域语义不能直接抄编辑器 | 审查中 |
| 10. HTML 显式绑定 | Python 提示词与校验、TS 运行时、消毒白名单 | 使用明确协议，避免从中文展示文案猜业务语义 | 已修复：浏览器解析语义、显式购物车、多搜索框、汇总权重 |
| 11. 会话与运行权限 | 路由、服务端快照、原子认领、工厂、晚到保存 | 多用户 Web 必须保留归属检查，不能照抄单用户本机诊断口 | 审查中 |
| 12. hooks / memory / doom-loop | 模型工具、持久化、事件反馈与退出条件 | 核对有实际调用，不能只注册名称 | 审查中 |
| 13. 架构扫描与约束 | package scripts、两侧扫描、清单、跨语言入口、生成图 | Cargo 强制边界与本仓静态闸的能力差异 | 审查中 |
| 14. 真实验收链 | 单元与契约、浏览器、恢复脚本、构建 | 明确受控接口和真实 LLM 的验证边界 | 通过受控验收；未跑付费 LLM |

## 发现与修复

本轮共修复问卷原子性、重复回执、HTML 解析/作用域、购物车猜测、汇总数量误乘、数字徽标误改、多搜索框接线、并行阶段错配、台账耗时冲突与部分页面补发。对照 grok-build 的问卷四种结果、阶段配对、工具钩子和自动架构生成链路均逐项核对；产品领域权限和多用户归属继续保留本仓 fail-closed 实现。

## 验证

Python 定向回归 73 条、问卷/控制/重试 46 条、交付面与绑定 38 条通过；前端 runtime 1,202 条通过；桌面 1280px 与手机 390px 真实 iframe 检查通过；build、arch:check 通过。全量 Python 运行在既有历史夹具/环境差异处中断，详见终端日志；未宣称全量无失败。未使用真实付费 LLM。
