/** Virtual terminal node id — projection only, never persisted in STATE.graph. */
export const WHYBUDDY_TERMINAL_NODE_ID = "whybuddy-terminal-delivery";

export type ProjectionDensity = "compact" | "detailed";

export const PROJECTION_DENSITY_STORAGE_KEY = "whybuddy:projection-density:v1";